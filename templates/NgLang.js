/**
 *
 *  Language file for $name.
 *
 *
 *  @author  $user
 *  @date    $date
 *
 */
(function(define) {
    "use strict";

    /**
     * Register the $name file class with RequireJS
     */
    define([], function() {

        var message = {
            something: 'something' //This might be modified by yourself
        };

        return message;

    });


}(define));
